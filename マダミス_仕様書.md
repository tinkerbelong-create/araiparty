# マーダーミステリー枠 仕様書 (v0.1 ドラフト)

あらいの遊び場に「マダミス」枠を追加する。ココフォリア的な共有ボード＋個人だけに見える
ハンドアウト(HO)を、既存プラットフォームと同じ server-authoritative 方式で実装する。

- 想定: **通話しながら画面だけ使う**(音声・テキストチャットはサイト外)
- 今回は1作品だが、**シナリオはJSONで差し替え可能**な汎用基盤にする

---

## 1. 配置(既存の作法に合わせる)

| 種類 | パス |
|---|---|
| ルール/進行ロジック | `mm_core.js` |
| socket.io モジュール | `mm_server.js`(`server.js` から `require('./mm_server.js')(io, {})`) |
| 画面 | `public/mystery/index.html` |
| シナリオ | `scenarios/<id>.json`(**サーバー側のみ**。public に置かない) |
| ポータル登録 | `public/index.html` の `GAMES` 配列に1件追加 |

- 招待コードは既存同様5文字、接頭辞 `M`(例 `MK3XQ`)
- BGM は `public/bgm.js` に `mystery` トラックを追加(静かな緊張系)

---

## 2. シナリオJSONスキーマ(ドラフト)

```jsonc
{
  "id": "sample",
  "title": "シナリオ名",
  "players": { "min": 4, "max": 6 },
  "duration": "約120分",
  "intro": "全員が最初に読む導入(公開)",
  "commonInfo": "事件概要・現場見取り図の説明など(公開)",

  "characters": [
    {
      "id": "c1",
      "name": "キャラ名",
      "catch": "一言紹介(選択画面に出す・公開)",
      "color": "#e04545",
      "icon": "🎩",
      "publicProfile": "全員に見える人物紹介",
      "handout": {                    // ★本人にしか送らない
        "story": "あなたの物語",
        "timeline": ["21:00 …", "21:30 …"],
        "secrets": ["あなたは○○を隠している"],
        "objectives": [{ "text": "犯人を当てる", "points": 3 }],
        "abilities": ["1回だけ○○できる"]
      }
    }
  ],

  "phases": [
    { "id": "read",  "type": "read",        "title": "HO読み込み",  "minutes": 10 },
    { "id": "talk1", "type": "discuss",     "title": "第1議論",     "minutes": 20 },
    { "id": "inv1",  "type": "investigate", "title": "調査フェーズ1","minutes": 15,
      "picks": 2, "cardSet": "set1" },
    { "id": "talk2", "type": "discuss",     "title": "第2議論",     "minutes": 20 },
    { "id": "vote",  "type": "vote",        "title": "投票",        "minutes": 5 },
    { "id": "ending","type": "ending",      "title": "エンディング" }
  ],

  "cards": [
    {
      "id": "k1", "set": "set1", "place": "書斎",
      "title": "血のついた手袋",
      "body": "…カードの中身(★取得者にのみ送る)",
      "share": "choice"   // "private"=本人のみ / "public"=取得と同時に全体公開 / "choice"=本人が公開を選べる
    }
  ],

  "vote": { "type": "culprit", "label": "犯人だと思う人に投票" },

  "endings": [
    { "id": "true", "when": { "topVote": "c3" }, "title": "真相解明", "body": "…" },
    { "id": "bad",  "when": "else",             "title": "迷宮入り",   "body": "…" }
  ],

  "truth": "真相解説(★投票終了後にのみ全員へ配信)",
  "afterwords": "あとがき(任意)"
}
```

---

## 3. 秘匿ルール(最重要)

サーバーからクライアントへ**送らない**もの:

1. 他プレイヤーの `handout` 一式
2. `truth` / `endings` の本文(投票締切前)
3. まだ誰も取得していない `cards[].body`
4. 他人が `private` で取得したカードの `body`(存在・場所・取得者名だけ公開)

→ 送信は「公開ビュー(`pub`)」+「本人ビュー(`priv`)」の2本立て。既存ゲームと同じ
`broadcast()` パターンを踏襲する。

---

## 4. 画面(public/mystery/)

- **ロビー** — 部屋作成/参加、シナリオ選択、人数チェック、キャラ選択(早い者勝ち or ホスト割当)
- **共有ボード(ココフォリア的)** — 全員のキャラパネルを並べ、公開情報カードが場に積まれる
- **自分のHO** — 常に開けるサイドタブ。物語 / タイムライン / 目的 / 秘密
- **調査** — 場所ごとにカードが伏せて並ぶ。残り取得回数を表示。`choice` は取得後に「公開する」ボタン
- **メモ** — 自分だけのメモ欄(localStorage)
- **進行バー** — 現フェーズ名と残り時間。ホストは「延長 +5分 / 次へ進む」操作可
- **投票 → エンディング** — 集計結果、分岐エンディング、真相解説、各自の目的達成判定

---

## 5. 決定事項(v1)

- [x] シナリオ第1弾 = 『ケーキを食べた能力者』(3人用) → `scenarios/cake.json`
- [x] キャラ選択は**早い者勝ち**(選び直し可)
- [x] **GM不要**。能力は「能力 × 対象」を選ぶ形式で、事前定義の応答をサーバーが返す
- [x] 進行はホストが「次のフェーズへ」を押す方式。タイマーは表示のみで自動では進めない(＋5分ボタンあり)
- [x] 能力フェーズは**全員そろったら自動で解決**して次へ
- [x] 最終フェーズは**選択式で自動採点**。能力秘匿は他人の回答から自動判定
- [x] 切断はCPU代行なし。招待コード＋同じ名前で復帰できる

## 6. 能力の自動応答(★araiが書いたものではないので要チェック)

原作ではGMが即興で処理する部分を、以下のように構造化しました。`cake.json` の
`abilityActions` / `places` で定義しているので、文言も効果も自由に変えられます。

| 能力 | 対象 | 結果 |
|---|---|---|
| ナツキ「すり抜け」 | 場所 | その場所の手がかりを得る。**暗い場所には使えない**(冷蔵庫は電源が抜けたままなので常に失敗し、ミッション④のヒントになる) |
| ナツキ「コピー」 | 人＋能力 | 言い当てられたら成功。**次の能力フェーズでその能力を1回使える**(使用回数を消費しない) |
| ナツキ「同化」 | 場所 | 手がかり＋**同じフェーズにその場所へ来た人が分かる** |
| リュウセイ「水」 | — | フレーバーのみ |
| リュウセイ「偽物」 | 物＋場所 | 偽の証拠を設置。**他人がその場所を調べると見つかる** |
| リュウセイ「カメラ」 | 場所 | 来た人が分かる。**暗い場所では「誰かがいる」までしか分からない** |
| カイト「ドア」 | ドア | 開閉。冷蔵庫の扉は明るさに影響する(ただし電源が抜けているので効果なし) |
| カイト「キーワード」 | 人 | まだ知らない能力のキーワードを1つ入手 |
| カイト「消す」 | — | **月1回制限で使用不可**(22:20にケーキを消したため) |

解決順は ①ドア → ②偽物設置 → ③カメラ → ④覗き見/同化 → ⑤キーワード/コピー。
これで「カイトが扉を閉めた結果ナツキが失敗する」といった原作の再現が成立します。

## 7. 採点の対応表

| キャラ | ミッション | 判定 |
|---|---|---|
| ナツキ | ケーキを食べた犯人(5) | Q「ケーキを食べた犯人は誰?」でリュウセイと回答 |
| ナツキ | 宝石の行方(3) / 取れなかった原因(1) | 選択式の正解 |
| リュウセイ | ばれない(5) | 他の2人の誰からも「リュウセイ」と指されない |
| リュウセイ | 偽物を食べた人(3) / 音の正体(各2) | 選択式の正解 |
| カイト | ばれない(5) | 他の2人の誰からも「カイト」と指されない |
| カイト | まずかった原因(3) | 選択式の正解 |
| カイト | 相手の能力を当てる | 正解2つにつき1pt |
| 全員 | 能力がバレない(×2) | 他の誰か1人でも正しく指摘したらバレ扱い |

## 8. 動かす

```bash
npm start          # http://localhost:3000/mystery/
node test_mm.js    # 秘匿性・能力解決・採点のテスト(49項目)
```
